mod bounds;
mod fast_funcs;

use rust_decimal_macros::dec;
use rust_decimal::Decimal;
use rust_decimal::MathematicalOps;


fn main()
{
    //let root_2 = fast_funcs::sqrt(dec!(2));
    //bounds::init();
    //println!("{}", root_2);
}
